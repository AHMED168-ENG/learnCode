import Controller from "./main.controller"
import {Request, Response, NextFunction} from "express"
import httpStatus from "http-status"

export class SponserController extends Controller {
  constructor() {
    super()
  }
}
